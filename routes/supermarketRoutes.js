const express=require('express');
const Supermarket=require('../models/supermarket');
const router=express.Router();
router.get('/:slug/dashboard',async(req,res)=>{const supermarket=await Supermarket.findBySlug(req.params.slug);if(!supermarket)return res.status(404).render('error',{title:'Supermarket not found',message:'This supermarket is unavailable.'});res.render('supermarket-dashboard',{title:`${supermarket.name} dashboard`,supermarket,map:await Supermarket.map(supermarket.id)});});
router.post('/:slug/products',async(req,res)=>{const supermarket=await Supermarket.findBySlug(req.params.slug);if(!supermarket)return res.sendStatus(404);const product={name:String(req.body.name||'').trim(),category:String(req.body.category||'General').trim(),sku:String(req.body.sku||'').trim(),price:Number(req.body.price||0),stock:Number(req.body.stock||0),bay:Number(req.body.bay||1),shelf:String(req.body.shelf||'Eye level').trim(),aisleId:Number(req.body.aisleId)||null,x:Number(req.body.x||0),y:Number(req.body.y||0)};if(!product.name||product.price<0||product.stock<0)return res.status(400).render('error',{title:'Invalid product',message:'Provide a valid product name, price and stock level.'});await Supermarket.createProduct(supermarket.id,product);res.redirect(`/supermarket/${supermarket.slug}/dashboard`);});
router.post('/:slug/products/:id/delete',async(req,res)=>{const supermarket=await Supermarket.findBySlug(req.params.slug);if(supermarket)await Supermarket.removeProduct(supermarket.id,Number(req.params.id));res.redirect(supermarket?`/supermarket/${supermarket.slug}/dashboard`:'/');});
module.exports=router;
