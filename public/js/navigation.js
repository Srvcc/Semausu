document.addEventListener('DOMContentLoaded',()=>{const root=document.querySelector('[data-navigation]');if(!root)return;document.querySelector('#buildRoute').addEventListener('click',async()=>{const productIds=[...document.querySelectorAll('.menu-item input:checked')].map(input=>Number(input.value));if(!productIds.length)return alert('Select at least one product.');const response=await fetch(`/api/supermarkets/${root.dataset.slug}/route`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({entranceId:Number(document.querySelector('#entrance').value),productIds})});const route=await response.json();document.querySelector('#routeLine').setAttribute('points',[route.start,...route.stops].map(point=>`${point.x},${point.y}`).join(' '));document.querySelector('#routeStops').innerHTML=route.stops.map(stop=>`<li><b>${escapeHtml(stop.name)}</b><small>${escapeHtml(`${stop.aisle_code || ''}, bay ${stop.bay}, ${stop.shelf}`)}</small></li>`).join('');});});function escapeHtml(value){const node=document.createElement('span');node.textContent=value;return node.innerHTML;}
